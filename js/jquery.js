// Testing JavaScript if is calling in the page.
console.log("OK!");
// JQuery
$(document).ready(function() {
    $('.sidenav').sidenav();
    $('.materialboxed').materialbox();
    $('.tabs').tabs();
    $('.tooltipped').tooltip();
    $('.scrollspy').scrollSpy();
    $('.modal').modal();
});
